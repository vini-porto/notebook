[B] What is a hybrid language in the context of programming? A language that is both compiled and interpreted — code is first compiled to an intermediate format, then interpreted at runtime (e.g., Java) [B] What is bytecode in Java? An intermediate, platform-independent binary format produced by the Java compiler (javac) that the JVM then executes [B] What is the JVM? Java Virtual Machine — the runtime engine that executes Java bytecode on any platform [B] What is the JDK? Java Development Kit — a toolkit containing the JVM, compiler (javac), and other tools needed to develop and run Java programs [B] What is static typing in Java? A type system where every variable's type must be declared at compile time and cannot change [B] What is a compile-time error? An error caught by the compiler before the program runs, typically caused by syntax mistakes or type mismatches [B] What is a run-time error? An error that occurs while the program is executing, after it has compiled successfully [B] What is a logic error? A bug where the program runs without crashing but produces incorrect results due to flawed instructions [B] What is type inference in Java? The compiler's ability to automatically determine a variable's type from its assigned value, enabled by the `var` keyword [B] What is JShell? A REPL (Read-Eval-Print Loop) tool in the JDK for quickly testing Java code without writing a full program [B] What are primitive types in Java? Basic data types that store raw values and have no methods — e.g., int, double, boolean, char [B] What are object types in Java? More complex types that can have behaviors (methods) — e.g., String, ArrayList [B] What is a Java array? A fixed-length collection of values that must all be of the same type [B] What is a compiled language? A language where source code is converted by a compiler into a machine-ready executable for a specific platform before execution (e.g., C++, Rust) [B] What is an interpreted language? A language where code is executed directly by an interpreter at runtime without prior compilation (e.g., Python, JavaScript) [T] What command compiles a Java source file into bytecode? javac [T] What keyword in Java allows the compiler to infer a variable's type? var [T] What tool in the JDK provides a REPL environment for Java? jshell [T] What character must end every Java statement? ; [T] What intermediate format does javac produce? bytecode [T] What acronym describes the runtime engine that executes Java bytecode? JVM [T] What acronym describes the full toolkit needed to develop Java programs? JDK [T] What Java primitive type stores true/false values? boolean [T] What Java primitive type stores a single character? char [T] What Java primitive type stores a 64-bit floating-point number? double [T] What Java primitive type stores a 32-bit whole number? int [T] What syntax is used for a single-line comment in Java? // [T] What syntax is used for a multi-line comment in Java? /* */ [T] In Java, what wraps a block of code such as a loop body or method body? {} [T] What class in Java represents a resizable list of objects? ArrayList [C] Java source code is first compiled by {{c1::javac}} into bytecode, then executed by the JVM. [C] The {{c1::JVM}} allows Java bytecode to run on any device regardless of the underlying platform. [C] In Java, you declare an integer variable like this: {{c1::int}} x = 42; [C] Modern Java allows {{c1::var}} x = 42; where the compiler infers the type automatically. [C] Java arrays have a {{c1::fixed}} length and can only hold one type of data. [C] A {{c1::compile-time}} error is caught before the program runs; a run-time error occurs during execution. [C] In Java, code blocks are wrapped in {{c1::curly braces}} ({ }). [C] Every Java statement must end with a {{c1::semicolon}} (;). [C] Java is {{c1::case-sensitive}}, so `foo`, `Foo`, and `FOO` are treated as different identifiers. [C] {{c1::Primitive}} types in Java store raw values and have no methods, while object types can have behaviors. [C] The JDK contains three key tools: the {{c1::JVM}} (java), the compiler (javac), and JShell (jshell). [C] A {{c1::logic}} error means the program runs without crashing but produces the wrong output. [C] String and ArrayList are examples of {{c1::object}} types in Java. [C] In a compiled language like C++, source code is converted to a {{c1::machine-ready executable}} before running. [C] Java's `var` keyword enables {{c1::type inference}}, but the variable remains statically typed. [S] int x = 42; // System.out.println(x); Prints: 42 [S] var name = "Alice"; // System.out.println(name.toUpperCase()); Prints: ALICE — var infers String type; String methods are available [S] int[] nums = new int[3]; // nums[0] = 10; nums[1] = 20; nums[2] = 30; // System.out.println(nums[1]); Prints: 20 [S] int x = 5 // System.out.println(x); Compile-time error — missing semicolon after `int x = 5` [S] String s = null; // System.out.println(s.length()); Throws NullPointerException at runtime — cannot call methods on null [S] int[] arr = new int[3]; // System.out.println(arr[5]); Throws ArrayIndexOutOfBoundsException at runtime — index 5 exceeds array length 3 [S] int x = 10; // int y = 3; // System.out.println(x / y); Prints: 3 — integer division truncates the decimal; result is not 3.33 [S] boolean flag = true; // if (flag) { // System.out.println("yes"); // } else { // System.out.println("no"); // } Prints: yes [S] int x = "hello"; Compile-time error — type mismatch: cannot assign a String to an int variable [S] var x = 10; // x = "text"; Compile-time error — var infers int at declaration; reassigning a String violates static typing [S] // This is a comment // int x = 5; // System.out.println(x); Compile-time error — int x = 5 is commented out; x is undefined [S] int x = 7; // int y = 2; // System.out.println(x % y); Prints: 1 — % is the modulo operator returning the remainder [S] char c = 'A'; // System.out.println(c); Prints: A — char stores a single character in single quotes [S] int x = 5; // double y = x; // System.out.println(y); Prints: 5.0 — int is automatically widened to double (implicit casting) [S] double x = 9.99; // int y = (int) x; // System.out.println(y); Prints: 9 — explicit cast truncates the decimal, does not round